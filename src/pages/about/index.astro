---
import BaseLayout from '@/layouts/BaseLayout.astro';
import Breadcrumbs from '@/components/Breadcrumbs.astro';
import { siteConfig } from '@/config/site';
---

<BaseLayout title="关于我" description="关于作者与本站的说明。"
  ><div class="container">
    <Breadcrumbs items={[{ label: '关于我' }]} /><header class="page-header">
      <p class="eyebrow">关于</p><h1>{siteConfig.author}</h1><p>
        这是一个等待作者补充的个人简介页面。为避免虚构，单位、职称、研究方向、联系方式与真实经历暂不填写。
      </p>
    </header><article class="prose">
      <h2>本站用途</h2><p>
        用于发布课程信息、教学资源、博客日志和项目记录。网站使用 Astro
        生成纯静态页面，不需要数据库或持续运行的服务器。
      </p><h2>内容说明</h2><p>
        目前的课程、文章、资源和项目均明确标为演示内容。请参考 <code>docs/</code> 中的维护指南逐步替换。
      </p><h2>联系</h2>{
        siteConfig.email ? (
          <p>
            邮箱：<a href={`mailto:${siteConfig.email}`}>{siteConfig.email}</a>
          </p>
        ) : (
          <p>
            暂未配置联系邮箱。可在 <code>src/config/site.ts</code> 中填写。
          </p>
        )
      }<p>
        <a href={siteConfig.githubProfile} target="_blank" rel="noopener noreferrer"
          >GitHub 主页（外部链接）</a
        >
      </p><h2>隐私</h2><p>
        第一版不启用统计、评论、登录或表单，不收集搜索词与访客个人信息。若以后加入隐私友好的统计服务，应先更新本说明并评估数据范围。
      </p>
    </article>
  </div></BaseLayout
>
